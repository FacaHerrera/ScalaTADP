package domain.entities.distribucionesDeProbabilidad

import domain.entities.{DistribucionDeProbabilidad, Suceso, Resultado}

//class DistribucionEquiprobable(sucesos: Set[Suceso]) extends DistribucionDeProbabilidad(sucesos) {
//  var probabilidad: Float = 100 / sucesos.size
//  resultados = sucesos.map(suc => (suc, probabilidad))
//}
